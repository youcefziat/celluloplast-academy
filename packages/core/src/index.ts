/**
 * Public surface of `@cio/core`.
 *
 * Consumers reach into the package via subpath imports (`./services/*`, `./utils/*`,
 * `./config/*` — see `package.json` exports). Nothing is re-exported at the top level.
 */

export {};
